# Copiar a .env y rellenar con valores reales. No commitear .env
# cp .env.example .env

# Base de datos
DB_HOST=host.docker.internal
DB_PORT=3306
DB_USER=root
DB_PASSWORD=cambiar_por_contraseña_segura
DB_NAME=ionlifebd

# JWT: generar una clave larga y aleatoria (ej: openssl rand -base64 32)
JWT_SECRET=cambiar_por_clave_jwt_larga_y_aleatoria

# Usuario administrador inicial
ADMIN_EMAIL=admin@ionlife.local
ADMIN_PASSWORD=cambiar_por_contraseña_admin

# API (frontend)
VITE_API_URL=http://10.0.0.3:18081
